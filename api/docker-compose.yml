version: '3.8'

services:
  api:
    build: .
    container_name: neelstate-api
    ports:
      - "8003:8003"
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
    command: npm run dev
